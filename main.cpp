using namespace std;

#include <iostream>
#include "src/Functions.cpp"

int main() {
    printGrid(requestInt("Qu'elle va être la hauteur de la grille ? >>"), requestInt("Qu'elle va être la largeur de la grille ? >>"));
    return 0;
}


